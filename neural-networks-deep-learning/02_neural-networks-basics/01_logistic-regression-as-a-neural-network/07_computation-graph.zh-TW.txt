您曾聽我說神經網路的計算是 組織成正向路徑或者說正向傳播步驟 也就是我們計算神經網路的輸出 接著是反向路徑或者反向傳播步驟 也就是我們用來計算梯度或者說計算導數 計算圖形解釋了為什麼組織成這樣 在這段影片, 我們將討論一個例子 為了要說明計算圖形 我們用比起羅吉斯迴歸分析或者完整的神經網路更簡單一點的例子 假設我們試著計算函數 J 也是一個函數有三個變數 a, b, 跟 c, 假設這個函數是 3(a+ bc) 計算這個函數實際上有三個不同步驟 首先您需要計算什麼是 bc 假設我們儲存這個在變數 u 所以 u = bc, 然後您也許計算 v = a + u 假設這是 v, 最後 您的輸出 J 是 3v 所以這是您您試著要計算的最終函數 J 我們可以拿這三個步驟把它們畫成計算圖形如下 假設我畫三個變數 a, b 跟 c 第一件事我們要做的是計算 u = bc 我放一個長方形盒子框起來 所以輸入會是 b 跟 c, 然後 您也許會有 v = a + u 所以這個輸入會是 v, 輸入會是 u 跟一起計算的 a 最後我們有 J = 3 v 所以用一個具體的例子, 如果 a = 5 b = 3, c = 2 那 u = bc 會是等於 6 因為 a + u 會是 5 + 6 是 11, J 是 3 成那個, 所以 J = 33 真的, 希望您可以自己檢查 這是 3 乘 5 加 3 乘 2 如果您展開它 您真的會得到 33, J 的值 所以計算圖形很方便, 當有 一些突出或者特殊的輸出變數 就像這個例子的 J 您想要最佳化 而在羅吉斯迴歸分析的例子 J 當然是成本函數我們試著要最小化 我們看到這個小例子是 經由從左到右的路徑 您可以計算 J 的值 我們在下幾張投影片會看到, 為了計算導數 會有從右到左的路徑 像是藍色箭頭的相反方向 會是最自然的方式來計算導數 總結一下, 計算圖形組織了 一個計算就是藍色箭頭, 從左到右計算 讓我們進入下一段影片看看您如何可以 反向紅色箭頭從右到左計算導數 讓我們進入下一段影片