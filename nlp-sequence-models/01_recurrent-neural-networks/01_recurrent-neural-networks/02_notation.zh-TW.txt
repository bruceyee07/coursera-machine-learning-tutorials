在上一段影片中, 您可以看到 序列模型可以廣泛用在許多應用上 我們從定義建立序列模型時會用到的符號開始 讓我們看個例子 假設你想建立一個序列模型來輸入這樣的句子 「哈利波特和妙麗格蘭傑發明了新的咒語」
(Harry Potter and Hermione Granger invented a new spell.) 用單字組成 從JK 羅琳所著之《哈利波特》系列小說中節錄 假設你想要一個可以自動告訴你 人名出現在句子中何處的序列模型 這是個叫做「命名實體辨識」(Named-entity recognition) 的問題 常用於搜尋引擎當中 例如, 標示出過去一天出現在新聞文章中的人名 並適當予以索引 這個辨識系統可以運用於 找出文本中的人名, 公司名稱, 時間, 地點, 國家名稱, 貨幣名稱等等 現在, 給定這個輸入 x, 假設你想要一個模型 來輸出 y, 且一個輸入對應一個輸出 而目標輸出 y 會告訴你每個輸入單字是否為人名的一部分 從技術上來說
這也許不是最好的輸出表示方法 有一些更複雜的輸出表示法不只能告訴你 那個字是否為人名的一部分 也能告訴你各個人名在句子當中開頭和結尾的位置 像是知道哈利波特從這裡開始 並在這裡結束, 開始於此並結束於此 但在這個例子中 我會使用這個較為簡要的表示法 現在, 輸入是九個單字的序列 因此, 最終我們將有九組特徵來表示這九個單字 而位置和序列的索引 我將會用 X, 上標角括號中1到9 來表示他們的位置 X^<t>中的t就是位置索引 標示在句子中的位置 索引 t 指的是無論這些序列 是否為暫時 (temporal) 的序列 我都會用 t 來表示序列的索引 同樣的對於輸出 我們以 y 來表示, 索引為1, 2, 3, 到9 我們同樣用T_x來表示輸入序列的長度 在這個例子中有 9 個單字 T_x等於9, 而T_y表示輸出序列的長度 在本例中, T_x 等於 T_y, 但 在上一段影片中的 T_x 和 T_y 可以是不同的。 我們過去一直在使用的符號中 X^(i)是用來表示這是第i個訓練例子 所以, 第 i 個訓練例中第 t 個元素我們將用 X^(i)<t> 來表示 而Tx為序列長度 且訓練集中的不同例子可以有不一樣的序列長度 所以Tx^(i)表示第 i 個訓練例子的序列長度 同樣的, y^(i)<t>表示第 i 個訓練例中第 t 個輸出 Ty^(i)表示第 i 個訓練例子輸出序列的長度 在這個例子中 Tx^(i)為9, 但若是在另一個含有15個單字的訓練例子中 Tx^(i)就等於15 現在, 我們將開始實踐 NLP 或稱之為自然語言處理 這是我們第一次認真的見識這個程序 我們需要決定的一件事是 如何表示序列中的各個單字 像是你如何代表一個像「哈利」(Harry) 這樣的單字 還有X^<1>應該是甚麼 接下來我們將討論如何在一個句子中表示單獨的單字 用來表示一個單字在句子中，首要做的事是 取得一個字彙集 或稱之為字典, 意思是 一份你會用到的單字的清單 字彙集中第一個單字是"a" 是字典中第一個單字 第二個單字是"Aaron", 之後會有"and" 然後會有"Harry"和"Potter" 也許最後一個單字是"Zulu" 所以"a"是第一字 "Aaron"是第二字 "and"出現在字典中的位置為367 「哈利」出現在位置4075 「波特」在位置6830 而「祖魯」(Zulu) 是詞典的最後一個單字, 也許是位置10,000 在這個例子中 我要用一萬字大小的字典 對於現代 NLP 應用來說這是相當小的 對於商業應用, 對於視覺商業應用, 字典的大小為三到五萬更常見, 十萬也並不少見。 然後一些大型網際網路公司會用 可能是一百萬字甚至更大的字典 但你可以看到很多商業應用使用字典的大小 大約在三到五萬 但我要用一萬作為例子, 因為它是個整數。 所以, 如果你選擇了一萬個單字的字典, 建構 這個字典的方法可以是 從你的訓練集中選取最常用的一萬字 或是查看一些線上字典會告訴你 最常被存用的一萬個英文單字為何 然後用one-hot表示法來表達這些單字 例如, x^<1> 代表的詞為"Harry", 將是一個位置4075為1, 其他為0的向量 因為那個位置是"Harry"在字典中的位置 同樣的, x^<2>是一個只有在位置6830為1 其他位置為0的向量 "and"在位置367, 所以x^<3> 是只有在位置367為1其他為0的向量 每一個向量的維度都是10,000 因為字彙集中有一萬字 而這個"a"是字典中的首字 所以代表了"a"的x^<7> 會是第一位置為1其他位置為0的向量 因為它是字典中的首字 在這個表示法中 x^<t>對於不同 t 值都是個one-hot向量 one-hot 因為它只有一個1, 其他位置都是0 而你會有九個這樣的向量來表示句子當中的九個單字 我們目標就是輸入以這種方法表示的x 用一個序列模型去學習映射至目標輸出y 我會把它當做一個監督式學習問題, 需要輸入有標籤過資料x與y 最後一個細節, 也是之後影片會提到更多 如果你遇到一個不在你的詞彙集中的單字該怎麼辦？ 可以做的事是創建一個新標記或假字詞
稱為『未知』(unkown) 單字 我用<UNK>來表示沒有出現在字彙集中的所有單字 我們之後會回頭講這件事 總結這段影片 我們描述了一個符號方法來描述 序列資料訓練集中的x和y 在下一影片中, 我們會開始描述 將 X 對應至 Y 的遞迴神經網路