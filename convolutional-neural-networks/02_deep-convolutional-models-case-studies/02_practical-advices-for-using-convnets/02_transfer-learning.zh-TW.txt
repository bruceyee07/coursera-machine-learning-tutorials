如果您建置一個電腦視覺應用，與其 從頭開始訓練權重，從隨機初始開始 您通常會有較快的進展，如果您下載 從別人已經訓練好的權重 到網路架構上，使用這種預先訓練 然後移轉到您或許有興趣的新的任務上 電腦視覺研究社群一直都很會發表 很多的資料集在網上，如果您聽過像是 ImageNet, MS COCO, 或是 Pascal  type 等資料集 這些都是人們在線上發表的不同資料集 很多的電腦視覺研究者都已經
在上面訓練過他們的演算法 有時候這樣的訓練可達幾週，也許用了很多的  ＧＰＵ 而別人做過 且經歷了痛苦尋找高績效的過程 意思是您可以下載這些開源的權重，別人已經花了很多星期 甚至很多月的努力，您可以直接用來 在您自己的神經網路上很好的初始 而使用轉移學習來轉移一些知識從 相當大的資料集到您自己的問題上 我們來詳細看一下怎麼做 我們先從一個例子開始 假設您想建立一個貓偵測器
來辨識您自己的寵物貓 根據網路 Tigger 是常用的貓名字，Misty 也是另一個常用的貓名 假設您的貓們叫做 Tigger 跟 Misty 還有都不是 所以您有一個分類問題有三個分類 這個圖片是 Tigger 還是 Misty, 或者都不是 所有情況下，您的貓們都會在照片中 您或許沒有很多 Tigger 或 Misty的照片，您的訓練集可能很小 您怎麼辦？ 我建議您到線上下載一個開源的神經網路建置 不只下載程式，也要下載權重 有許多您可以下載的網路已經訓練過，舉個例 ImageNet 資料集有上千種不同的類別，這些網路 或許有一個 softmax 單元可以輸出上千種可能的類別 您可以去掉這個 softmax 層，建立 您自己的 softmax 單元，輸出為 Tigger 或 Misty 或都不是 至於這個網路 我鼓勵您把這些層 凍結，您凍結這些參數 在這個網路上的這些層，您會只 訓練您的 softmax  層的相關參數 這個 softmax 層會有三個可能輸出 Tigger, Misty 跟皆不是 使用他人的訓練過的權重 您或許會得到相當好的績效即使您的資料集很小 幸運的是很多深度學習框架 支援這樣的運作，實際上 根據不同的框架，它有可能用一種稱為可訓練的參數為 0 您或許可以把一些層做這樣設定 為了指名 不要訓練這些權重，有時候您會有一些參數 像是 freeze (凍結）等於 1 不同的深度學習框架有不同的方式，讓您 指定是否要訓練某特定層的權重 在這個例子，您只需要訓練 softmax 層的權重，但要凍結其他早期層的權重 另一個巧妙的技巧會許會幫助一些建置 因為所有早期層凍結 有一些固定的函數並不會改變，因為您並未改變它 您並沒有訓練它們，拿輸入的影像 對應到那一層的一組啟動值 一種技巧可以加快學習是我們可以預先計算那一層 那一層的特徵啟動值，將它們存在硬碟上 您要做的是使用這個固定函數 這個神經網路的第一部分 拿這個輸入任何影像，對它計算一些特徵向量然後 將這個特徵向量在淺層的 Softmax 模型中訓練，來做預測 一個步驟可以幫助您計算，就是您預先計算那些層的啟動值 對於所有在訓練集的例子，儲存它們到硬碟 然後就只要使用 softmax 分類器到它們上面 儲存到硬碟或者 預先計算的好處是您不需要 每次都重新計算這些啟動值 您做一次，或遍歷一次資料集 如果您任務中的訓練集相當小的話，您可以這樣做 如果您的訓練集比較大的話呢？ 一個經驗法則是，如果您有 一個較大的資料集，
或許有一堆 Tigger 或 Misty 的照片 跟一些兩者都不是的照片 您可以做的是凍結比較少層 或許您只凍結這些層，而訓練這些晚期的層 當然輸出層會使用不同的分類，您必須使用 您自己的輸出單元， Tigger, Misty 或者兩者皆不是 有幾種方式可以這麼做 您可以拿最後這幾層的權重 用它們當初始值，從那裡開始做梯度下降 或者您可以完全去掉最後幾層 使用您自己新的隱藏單元跟您自己最終的 softmax 輸出 兩種方式都可以試試 但或許模式是如果您有更多的資料 您需要凍結的層會變小 您要在上面訓練的層會變多 主要的想法是如果您選一個資料集，或許有 足夠資料不只是用在訓練單一個  softmax 單元，甚至可以訓練 一個夠大的神經網路包含 最後網路的最後幾層，您也可以使用 最後，如果您有很多資料 另一種方式是您拿這個開源的網路跟權重， 使用全部東西為初始，訓練整個網路 當然這是一個上千個節點的 softmax， 而您只需要 3 個輸出  您需要您自己的  softmax 輸出 您關心的輸出標籤 但如果對於您的任務，您有更多的標籤資料 或者您有更多的 Tigger, Misty 或都不是的照片 您可以訓練更多層，極端情況下 您可以使用您下載的權重只當成 初始值，它們用來取代 隨機初始，然後可以使用梯度下降 訓練所有的權重跟網路所有層 這就是卷積網路的轉移學習 實作上，因為網路上的開源資料集如此巨大，而您可以 下載別人已經花過幾個星期訓練在這麼多資料的權重 您會發現很多的電腦視覺應用 如果您下載 別人開源的權重，使用在您自己問題的初始值上，會表現得很棒 在其他不同的領域中 在深度學習的不同應用中 我認為在電腦視覺上是一個 您幾乎必須要做轉移學習的領域，除非 您有特別大的資料集，您自己要從頭訓練起 但轉移學習值得認真考慮，除非您 您有特別大的資料集， 跟很多的計算預算 來完全從頭自己訓練